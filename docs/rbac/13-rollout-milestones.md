# Rollout Milestones

1) Admin seeded and can sign in
2) Parent signup enabled (Google + Email/Password), dummy landing pages
3) API middleware and GET /me running
4) Admin invite creation (doc-only v0), accept flow (manual v0)
5) UI guards active; basic role-based navigation in place
6) Formalize invite email + token flow

